/** URL pública do site — base de canonical, og:url e sitemap. */
export const SITE_URL = 'https://victor-kaue.vercel.app';

// Previews de link: JPEG quadrado 280px gerado por `npm run images:optimize`.
// Quadrado e pequeno = WhatsApp mostra miniatura à esquerda do título.
export const DEFAULT_OG_IMAGE = `${SITE_URL}/images/og/home.jpg`;
