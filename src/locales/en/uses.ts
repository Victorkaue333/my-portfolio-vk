/* Tela Setup & Ferramentas (/uses). */
import type base from '../pt-BR/uses';

const uses: typeof base = {
  uses: {
    heroMain: 'Setup &',
    heroAccent: 'Tools',
    heroSubtitle:
      'What I use day to day to write code, organize my work and ship projects.',
    footerLink: 'Setup & Tools',
    itemsCount_one: '{{count}} item',
    itemsCount_other: '{{count}} items',
    opensInNewTab: 'opens in a new tab',
    ctaText: 'Want to see these tools in action?',
    ctaButton: 'See projects',
  },
};

export default uses;
